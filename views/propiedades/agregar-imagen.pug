extends ../Layout/admin  

block styles


block contenido 
     div.py-10
            h1.text-4xl.my-10.font-extrabold.text-center Bienes 
                span.font-normal Raices
            h2.text-center.text-2xl.font-extrabold=pagina
            
            div(class="bg-white shadow py-8 px-5 rounded mx-auto max-w-4xl my-10 md:px-10")
                form.space-y-8(method="POST" action="/propiedades/crear")
                    input(type="hidden" name="_csrf" value=csrfToken)

                    input(class="w-full bg-indigo-600 py-3 text-white uppercase rounded text-center font-bold cursor-pointer hover:bg-indigo-700" type="submit" value="Publicar propiedad")


block scripts